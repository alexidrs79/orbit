# orbit

Custom WordPress theme for **Orbit**, built as its own repo from the start rather than split out of the shared `wp-themes` monorepo.

## Installing

Copy or symlink this folder into `wp-content/themes/` and activate it.

## Requirements

- WordPress 6.4 or newer, PHP 8.0 or newer
